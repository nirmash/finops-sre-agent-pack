# Generated by render_tasks.py from task-manifest.json and prompts/.
# Do not edit this file directly; run: python3 render_tasks.py write
apiVersion: azuresre.ai/v1
kind: ScheduledTask
metadata:
  name: "FinOps: Rightsizing Savings (Live Report, Weekly)"
  owner: "__ALERT_EMAIL__"
  tags:
    - finops
    - finops-live-report
    - finops-rightsizing-advisor
spec:
  name: "FinOps: Rightsizing Savings (Live Report, Weekly)"
  description: >-
    Part of the FinOps pack — a weekly-refreshed Live Report (Operations Hub) and review
    delivery for rightsizing / idle-resource savings: total potential savings, a top-
    opportunities chart, and ranked recommendations.
  agent: "__AGENT_NAME__"
  cron: "__RIGHTSIZE_REPORT_CRON__"
  modelTier: "ReasoningHeavy"
  agentPrompt: |
    STRICT MANAGED-SCOPE PREAMBLE — complete this before any Azure analysis query, email, ListReports/GetReport call, or SaveReport call.

    Load `finops-managed-scope`; follow its current SKILL.md, bundled `scope.py`, and deterministic helpers with agent resource ID `__AGENT_RESOURCE_ID__`. Dynamically GET the current `managedResources` and resolve them in scheduled mode. This task accepts no override. Broad RBAC or visibility must never expand the boundary.

    FAIL CLOSED if discovery is unavailable, malformed, empty, or cannot fully expand a configured management group: stop without querying analysis data, sending email, or saving a report, and return only the scope error.

    Use only the resolved effective scopes. Follow the skill contract for independent scoped retrieval, complete pagination, overlap de-duplication, exact UsageDetails transport filtering, and disclosure of included, excluded, unattributed, unsupported, and partial/failed coverage. Never infer a parent or substitute scope.

    Azure operations are read-only: GET/read commands only; never use Azure POST, PUT, PATCH, DELETE, or a write tool.

    Author and SAVE the static snapshot Live Report `FinOps: Rightsizing Savings` now; do not ask questions or defer to chat.

    1. Run the full read-only `finops-rightsizing-advisor` SKILL.md contract for every effective scope and use its deterministic `rightsize.py` output. Preserve evidence, scope/cost gaps, and `verify first` status for unvalidated rows.
    2. Call ListReports; call GetReport and reuse the exact-name report's `reportId`, or create it if absent.
    3. Load `finops-report-renderer`, its SKILL.md, exact `models.py`, and exact `render.py`. With `datetime.now(timezone.utc)`, call `build_rightsizing_savings_model(rightsize_result, ...)`; do not generically construct a model or write/edit HTML. Run `write_report(model, "finops-rightsizing-savings.html")`.
    4. SaveReport the generated static file with `allowedTools: []`, name `FinOps: Rightsizing Savings`, and the reused `reportId` when present. Make no view-time calls. Confirm the saved report id and version. If ListReports, GetReport, or SaveReport is unavailable or fails after the analysis succeeds, state that report delivery failed but continue step 5; a report-tool failure must never suppress the scheduled result or email.
    5. If no item clears the skill threshold, return exactly `No rightsizing opportunities above threshold this week.` and do not email. Otherwise return the ranked recommendations and total estimated monthly savings, then if `__ALERT_EMAIL__` is configured, email it with subject `Weekly rightsizing review — <date>` and Normal importance; otherwise return it only in the scheduled-task result.

    Recommend only; never modify resources.
